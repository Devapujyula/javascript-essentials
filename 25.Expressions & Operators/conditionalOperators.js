let age = 23;
let voteableAge = age < 18 ? "Too young" : "Old enough";
console.log(voteableAge);

let isLoggedIn = false;
let subscribe = isLoggedIn === true ? "LogIn" : "Logout";
console.log(subscribe);
